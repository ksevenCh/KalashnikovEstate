<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Форма входа</title>
</head>
<body>
<h2>Форма входа</h2>
<%= form_with(model: @user, url: '/enter') do |form| %>
  <div>
    <%= form.label :login %>
    <%= form.text_field :login %>
  </div>
  <div>
    <%= form.label :password %>
    <%= form.password_field :password %>
  </div>
  <div>
    <a>
    <%= form.submit "Войти" %>
    </a>
  </div>
<% end %>
<form>
  <%= link_to 'Регистрация', '/registration', method: :get %>
</form>
</body>

<style>

    a {
        text-decoration: none;
        color: #000;
        font-weight: bold;
        padding: 10px 20px;
        background-color: #f4f4f4;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    a:hover {
        background-color: #e4e4e4;
    }

    a:active {
        background-color: #d4d4d4;
    }

    body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        padding: 20px;
    }

    h2 {
        text-align: center;
    }

    form {
        max-width: 300px;
        margin: 0 auto;
        background-color: #fff;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    label {
        display: block;
        margin-bottom: 8px;
    }

    input[type="text"],
    input[type="password"] {
        width: 100%;
        padding: 10px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
    }

    button {
        width: 100%;
        padding: 10px;
        background-color: #9edca0;
        color: #000000;
        border: 3px solid #b4b4b4;
        border-radius: 5px;
        cursor: pointer;
    }

    button:hover {
        background-color: #85e58b;
    }
</style>

</html>